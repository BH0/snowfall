/*
s
Issues / Tasks:
Send options from HTML/PHP Widget form to JSON file - likely within widget-class
Make (AJAX) request to JSON - Likely within main.js file - done
    to get the user's desired values that were entered into the PHP Widget at
    Wordpress dashboard
Use such values as the variable values which dtermine the behaviour of the plugin - done

Notes:
The plugin/widget works as expected except the only issue that remains is
    saving user's desired values to the JSON file and getting these within the
    main.js (JS) file/script

Issue:
The main.js code is having diffulcty retrieving the desired data/values from
    the JSON file accurately

*/
